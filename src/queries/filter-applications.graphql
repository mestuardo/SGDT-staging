query Query($jobOfferId: ID!) {
  jobOfferApplications: filterApplicationsWithJobOfferId(jobOfferId: $jobOfferId) {
    id
    jobOfferId
    professionalId
    status
    recruiterId
    questions
    answers
    assessment
    stage
  }
}
