mutation CreateRequestMutation($createRequestInput: RequestInput) {
  createRequest(input: $createRequestInput) {
    internalRep
    client
    maxSalary 
    shiftType
    contractType   
    vacancies
    position
    stage
  }
}
